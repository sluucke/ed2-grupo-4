# TODO: implementar heurística de comprimento do título


def apply_heuristic(best_sentence, sentences, processed, top_scores) -> str:
    # contar palavras: len(best_sentence.split())
    # truncar: re.search(r"[,;]", sentence)
    # mini textrank: importar graph.build_graph e textrank.textrank
    # capitalizar primeira letra e remover ponto final no retorno 
    pass